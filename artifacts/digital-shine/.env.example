# Frontend (Vite) env example

# Server / build
PORT=3000
BASE_PATH=/

# API
# Used to configure Vite dev proxy target and any explicit API base URL.
# In this app, the contact form currently calls '/api/contact'.
# Still useful for deployments where you may want full API URL.
VITE_API_ORIGIN=https://digital-shine.onrender.com

